#!/usr/bin/env bash

export DB='test/db/db.h5'
rm -f $DB
coverage run --parallel-mode -m unittest "$@"
rm -f $DB
coverage run --source=mpr --parallel-mode -m behave -m
echo
coverage combine
coverage report
echo
