#!/usr/bin/env bash
export MPR_SERVER='http://localhost:8080'

set -e
lint
coverage run --parallel-mode -m pytest
coverage run --parallel-mode -m behave -m --tags="-pending"
echo
coverage combine
coverage report
echo
